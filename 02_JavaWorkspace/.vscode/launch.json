{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "JAVA Launch",
            "type": "extensionHost",
            "request": "launch",
            "launchOptionType": "Local",
            "targetArchitecture": "x86",
            "program": "${file}",
            "stopAtEntry": false,
            "cwd": "${workspaceRoot}",
            "externalConsole": true,
            "preLaunchTask": "cmd"
        }
    ]
}